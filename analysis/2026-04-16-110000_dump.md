# Brain Dump — 2026-04-16-110000
**Phase**: Critical bug likelihood assessment + open redirect documentation
**Session time**: ~5 min this block

## What I Just Did
- Extracted and documented all three open redirect surfaces from JS analysis
- Created candidates/open-redirect.md with OR-001/002/003
- Assessed critical (9.0+) vs high (7.0-8.9) likelihood across all confirmed surfaces

## Open Redirect Surfaces

### OR-001 — login goto (HIGHEST PRIORITY)
`layout_subaccounts_dropdown.js` → `getRedirectUrl()` → `window.location = data.results.goto`
No origin/scheme check. Server constructs the goto field. If any request param influences it,
this is a confirmed open redirect. Test: does `?next=`, `?return=`, `?goto=` appear in login POST
and does it echo into `data.results.goto`?

### OR-002 — stream_url (MEDIUM)
4 locations in reports JS. `window.location.replace(t.responseJSON.results.stream_url)`.
Format unknown — need to capture a real download-file PUT response to see if absolute or relative.

### OR-003 — login-soft goto (SAME AS OR-001)
`login-soft.js:86` identical pattern. Likely same server handler. Test together with OR-001.

## Critical Bug Likelihood Assessment

### What "Critical" requires (CVSS 9.0+)
- SSRF → cloud metadata credentials (IAM keys) — not reachable on Linode (4%)
- SSRF → internal service with RCE vector (Redis eval/Lua RCE)
- Full account takeover via session token exfil from Redis
- Pre-auth mass account takeover

### Realistic paths to Critical on smtp2go

| Path | Probability | What it needs |
|------|------------|---------------|
| SSRF → Redis session token → ATO | ~15% | Redis on localhost AND no egress block AND session in Redis |
| update-active-user → DoS all API keys for any account | ~35% | No ownership check (likely) |
| update-active-user → sandbox_mode=true on victim account | ~30% | No ownership check (likely) |
| Integer IDOR delete victim's template/webhook | ~25% | No ownership check on delete |

### Most realistic path to High (7.0-8.9)

| Bug | Confidence | CVSS |
|-----|-----------|------|
| SSRF full-response (internal service responds) | 38% | 7.7 |
| update-active-user blocks victim API key | 35% | 6.5 |
| Object IDOR (template/webhook delete) | 25% | 5.4–6.5 |
| SSTI Jinja2 in template delivery | 20% | 8.1 if RCE |
| Open redirect (if influenceable) | 20% | 6.1 |

### Overall assessment
- P(finding any High or above) ≈ 65–70%
- P(finding Critical 9.0+) ≈ 10–15%
- Most likely first confirmed high: SSRF internal response OR update-active-user IDOR

### Why not higher for Critical
- Linode metadata = low value (no IAM creds)
- Redis not confirmed on localhost — could be managed Linode DB cluster
- API key IDOR needs to prove ownership bypass, not just API key as lookup key
- SSTI surface requires Jinja2 on backend AND user-controlled input in template rendering context

## What's Most Likely to Pop First

**SSRF** (test it first) — full-response server fetch by design, test button exists,
no URL validation in client. If Redis/Gunicorn/Postgres respond → immediate High report.

**update-active-user IDOR** (5 minutes to test) — Account B session + Account A's full_api_key.
If `blocked: true` sticks, that's a confirmed IDOR with DoS impact.

## Next 5 Minutes

1. User has sessions ready — run webhook creation + interactsh baseline
2. While waiting for interactsh hit: run update-active-user cross-account test
3. Capture download-file PUT response to see stream_url format (OR-002)
