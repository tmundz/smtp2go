# Brain Dump — 2026-04-15-232000
**Phase**: IDOR testing — exhausting available non-subaccount surfaces
**Session time**: ~45 min on IDOR testing total

## What I Just Did
- Tested IDOR-010 (JWT forgery): JWT cookie ignored by smtp2go API — only for Zendesk chat. Closed.
- Fetched Account B team data: email=0xcaphe+ted@wearehackerone.com, hash_id=33c5af9ea8d32bcf..., member_id=1015003
- Tested cross-team actions from Account A targeting Account B's email (reinvite, delete-member, change-username): all return "login-required" — server scopes by team membership correctly
- Tested session-scoped endpoint param injection (account_id, member_id, username, hash_id params): all ignored, server uses session cookie for context
- Tested bounce_suppress: GET works, PUT returns 405 (wrong method for this endpoint)
- Tested suppression list with Account B's params: params ignored, returns Account A's own data

## What I Found

### IDOR-010 — Closed
JWT cookie `1XJgM5twoV0e8IhkwW0BQuDwzkz` is purely for Zendesk chat widget authentication.
Not used by smtp2go API for any authorization. alg:none forgery has no effect on API responses.

### Session-scoped endpoints — properly defended
All tested endpoints (team, suppression, bounce_suppress, changeplan, reports/summary) use the
`session` + `login_id` cookie exclusively for authorization. Explicit params like `account_id`,
`member_id`, `username`, `hash_id` are silently ignored — server returns the caller's own data
regardless of what ID params are passed.

### LOGIN_AS status — inconclusive (needs subaccounts)
Server accepts any integer via LOGIN_AS and returns "Login Successful" regardless.
Resulting tokens authenticate as the master account (fallback behavior).
Cannot confirm cross-tenant access without real subaccount record_ids.
Both test accounts are on free/TRIAL plan — subaccounts require paid upgrade.

### Account B identity confirmed
- Email: 0xcaphe+ted@wearehackerone.com
- Name: ted lasso
- Master account ID: 1015003 (from JWT external_id)
- hash_id: 33c5af9ea8d32bcf539414d5f11f6696ecb94a67dfb6b2ddaef59892e9e0201b

## What Failed / Dead Ends
- Subaccount IDOR (IDOR-001 through IDOR-006): blocked by free plan — subaccounts require upgrade
- JWT forgery (IDOR-010): JWT is for Zendesk, no effect on smtp2go
- Team action cross-account: server validates team membership before processing
- hash_id / account_id param injection: all params ignored
- Invoice ID enumeration: parameter format unknown, `invoice_id=<int>` ignored

## Current Hypothesis

The remaining testable IDOR surfaces require Account B to have created objects with numeric IDs:
1. Webhook IDs — Account B creates a webhook → Account A tries CRUD on that webhook ID
2. SMTP user IDs — same pattern
3. Template IDs — same pattern

These object-level IDORs are the classic "guess another account's integer ID" pattern.
They don't require paid plans. Account B just needs to create those objects.

WCP-006 (CORS + login_id token in response body) remains the highest confirmed severity.

## Next Steps
1. Account B: create an SMTP user or webhook → get numeric ID
2. Account A: try GET/PUT/DELETE on that ID
3. Also test SSRF — webhook URL field is a direct server-fetch surface (no domain needed)

## Open Questions
- Does the LOGIN_AS fallback actually represent a missing ownership check, or a true ownership check that falls back on failure?
- Are webhook/SMTP user IDs sequential integers or UUIDs?
