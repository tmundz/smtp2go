# Brain Dump — 2026-04-15-174200
**Phase**: CORS Analysis — COMPLETE
**Session time**: ~30 min

## What I Just Did
- Checked Vary headers and cookie flags on full response
- Enumerated api.smtp2go.com wildcard ACAO scope (all /v3/* /v2/* paths)
- Verified api.smtp2go.com has NO ACAC — public API by design, not exploitable
- Wrote candidates/cors.md with full documentation of both findings

## What I Found — Final Summary

### CORS-001: Static hotjar ACAO — High-value misconfiguration
**Status**: ✅ Confirmed
**Severity**: Medium–High (conditional)

Core facts locked down:
- `ACAO: http://insights.hotjar.com` + `ACAC: true` — STATIC, never changes
- All 13 origin probes returned identical ACAO (static whitelist, not reflection)
- 9 sensitive GET endpoints return data without CSRF key
- OPTIONS preflight missing `Access-Control-Allow-Methods` → POST exploitation blocked
- Cookies: no `Secure`, no explicit `SameSite` — defaults to Lax on modern browsers

Exploitability chain: XSS on insights.hotjar.com → read smtp2go authenticated API data
Limiting factor: SameSite=Lax default on Chrome 80+ / Firefox 79+

**Real impact**: readable data includes email addresses, full names, roles, member_id, plan details, subaccount IDs, dedicated IPs, verified senders, suppression list, webhook configs

### CORS-002: api.smtp2go.com ACAO: * — Info/Low
**Status**: ✅ Confirmed (by design, not exploitable)

## What Failed / Dead Ends
- No arbitrary origin reflection anywhere — static ACAO throughout
- Safari/special-char Phase 4 bypass: N/A (static ACAO, not regex-driven)
- POST endpoint CORS exploitation blocked by missing ACAM in OPTIONS
- `api.smtp2go.com` wildcard has no ACAC — no credentialed exploitation

## Current Hypothesis
This is a real finding worth reporting. The combination of:
1. Hardcoded `ACAO: http://insights.hotjar.com` on all sensitive endpoints
2. `ACAC: true` on all of them
3. No CSRF protection on sensitive GET reads
4. No `Secure` flag on cookies
5. The trusted domain being a third-party analytics service
...represents a meaningful privilege boundary violation if insights.hotjar.com is ever compromised or has XSS.

The SameSite=Lax default reduces real-world impact on modern browsers, but this should still be reported as Medium.

## Next Steps (if continuing)
- Search for XSS on insights.hotjar.com (out of scope for smtp2go but chains nicely)
- Check if any smtp2go endpoint sets `SameSite=None` explicitly (would elevate severity to High)
- Verify if program accepts CORS misconfiguration reports

## Open Questions
- None blocking — analysis complete
