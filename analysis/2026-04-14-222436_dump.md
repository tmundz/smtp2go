# Brain Dump — 2026-04-14-222436
**Phase**: Phase 2 — JS Analysis (app-us.smtp2go.com)
**Session time**: ~25 min

## What I Just Did
Ran all grep patterns from CLAUDE.md against 238 non-www JS files. Focused on:
`app-us.smtp2go.com/static/js/`, `static/vue/settings/`, `static/vue/team-app/`,
and the inline `(index).N.inline.js` files. Read key files in full:
`functions.js`, `layout.js`, `settings_general.js`, subaccounts/main.js,
api-auth/main.js, team-app/main.js, dashboard (index).3.inline.js.

## What I Found

### 🔴 CRITICAL — Login-As-Subaccount IDOR
**File**: `static/vue/settings/subaccounts/main.js:5860`

```js
onLoginAsClicked: function (t) {
  var e = {};
  e.LOGIN_AS = t.record_id;   // integer — the subaccount's numeric ID
  e.STACK = "STACK";
  var s = HTML.ajax("post", END_POINTS.urls()["Login"], e, ...);
  s.success(function (t) {
    window.location = END_POINTS.fronts()["Account Summary"];
  });
}
```
**Endpoint**: `POST /api/login/` with body `{ LOGIN_AS: <integer_id>, STACK: "STACK" }`
If `record_id` is an enumerable integer (like a database PK), Account A
can POST `LOGIN_AS=<B_record_id>` to log in as Account B's subaccount.

### 🔴 HIGH — Subaccount Management IDOR via `subaccount_member_id`
**File**: `static/vue/settings/subaccounts/main.js:5668–5858`

All subaccount management operations use `subaccount_member_id` (integer):
- `PUT /api/settings/subaccounts/?action=toggle-archiving` — enable/disable archiving
- `PUT /api/settings/subaccounts/?action=toggle-sms` — disable SMS
- `PUT /api/settings/subaccounts/?action=toggle-2fa` — toggle 2FA enforcement
- `PUT /api/settings/subaccounts/?action=open` — reopen closed subaccount
- `PUT /api/settings/subaccounts/?action=migrate` — migrate senders to subaccount
- `PUT /api/settings/subaccounts/?action=close` (inferred from SATBOX_REMOVE_CLICKED)

URL construction pattern: `/api/settings/subaccounts/?action=<action>&CSRF_key=<token>`

### 🟡 MEDIUM — Stored XSS: `$(".username").html(data.username)`
**File**: `static/js/functions.js:529`
```js
$(".username").html(data.username);   // .html() not .text()
```
Username from login API response is injected via jQuery `.html()`.
If the server returns a username containing HTML (e.g. stored at registration),
this fires on every dashboard page load.

Also: `loginMessage.append(data.login_message)` at line 541 — jQuery `.append()`
with a string parses HTML; if `login_message` contains `<img onerror=...>` it fires.

### 🟡 MEDIUM — `show_hidden_endpoints` URL param
**File**: `static/vue/settings/api-auth/main.js:1122`
```js
var a = new URLSearchParams(window.location.search);
var n = a.has("show_hidden_endpoints");
// n is then sent as { show_hidden_endpoints: n } in POST to load-active-users
```
Navigating to `/sending/apikeys/?show_hidden_endpoints` sends `show_hidden_endpoints: true`
to the backend, which may return additional hidden/admin API endpoints in the listing.

### 🟡 MEDIUM — `eval()` in functions.js
**File**: `static/js/functions.js:61,64`
```js
return eval("r." + x);   // line 61
return eval(x.slice(1)); // line 64
```
Context: a template/path resolver. `x` comes from caller — need to trace.
If `x` is ever populated from URL params or DOM values, XSS via eval.

### 🟡 MEDIUM — jQuery AJAX HTML injection (layout.js)
**File**: `static/assets/admin/layout/scripts/layout.js:179,218`
```js
pageContentBody.html(res);   // res = raw server HTML response
```
When sidebar nav links with class `.ajaxify` are clicked, the full HTML response
from the server URL is injected into the page content area. If any endpoint
returns user-controlled content unescaped, this is DOM XSS.

### 🔵 INFO — Complete API endpoint map (from (index).3.inline.js)
```
/api/dashboard/main/           GET  — dashboard data
/api/dashboard/billing_details/ GET — billing details
/api/dashboard/get_billing_history/ — billing history
/api/reports/summary/          POST — email summary
/api/reports/activity/         POST — email activity
/api/reports/archive/          POST — archived emails (HIGH sensitivity)
/api/reports/delivery/         POST — delivery reports
/api/reports/suppression/      POST — suppressions
/api/reports/data-exports/     POST — data exports
/api/settings/subaccounts/     GET/PUT — subaccount management
/api/settings/webhooks/        — webhook CRUD (SSRF surface)
/api/settings/team/            — team member management
/api/settings/api_users/       — API key management
/api/settings/smtp_users/      — SMTP users
/api/settings/verified_senders/ — sender management
/api/settings/templates/       — email templates
/api/settings/dedicatedips/    — dedicated IPs
/api/settings/archives/        — archive settings
/api/settings/ipallowlist/     — IP allowlist
/api/settings/two_factor_auth/ — 2FA settings
/api/account/close/            — account closure
/end-points/                   — full endpoint discovery (GET this unauthenticated?)
```

### 🔵 INFO — Team member operations use `username` not integer ID
DELETE member: `POST /api/settings/team/?action=delete-member` with `{ username: t.username }`
Reinvite: `POST /api/settings/team/?action=reinvite` with `{ username: t }`
Lower IDOR risk since usernames are typically email addresses.

### 🔵 INFO — API key operations use `username` too
DELETE API key: `POST /api/settings/api_users/?action=remove-active-user` with `{ username: i }`
Lower IDOR risk.

### 🔴 INFO — `record_id` value observed
From subaccounts/main.js:104 inline config:
`"sponsor": 1010395` — this is a master account ID. Subaccount `record_id` values
are likely in a similar numeric range.

## What Failed / Dead Ends
- No hardcoded secrets found in custom JS
- postMessage usages all trace back to ace.js (code editor library) and Vue router internals — not custom app logic
- SSRF URL fields not directly visible in custom JS (webhook URL fields likely in vanilla HTML forms, not Vue)
- No `window.opener` / `window.parent` usage in custom app code

## Current Hypothesis
**Highest priority**: `POST /api/login/` with `LOGIN_AS=<integer>` — if the backend
only checks that the master account owns the `record_id`, an enumeration/brute-force
of subaccount IDs across tenant boundaries could allow full account takeover of any subaccount.
The observed sponsor ID `1010395` suggests IDs are assigned sequentially.

**Second priority**: `subaccount_member_id` IDOR in PUT actions on `/api/settings/subaccounts/` —
allows toggling archiving, SMS, 2FA, reopening accounts for any subaccount if ID is enumerable.

**Third priority**: `$(".username").html(data.username)` — stored XSS if username is not
sanitized server-side on registration.

## Next 5 Minutes
Write candidates/idor.md and candidates/xss.md with full entries.
Then check CSV for actual `record_id` values seen in traffic.

## Open Questions
- Is `record_id` globally unique across all tenants, or scoped per master account?
- Does `/api/login/` with `LOGIN_AS` check that the caller owns the target subaccount?
- Is `login_message` from the server ever user-controlled (e.g., set at invite time)?
- Does `/end-points/` require auth?
- What does `show_hidden_endpoints=true` actually return that's different?
