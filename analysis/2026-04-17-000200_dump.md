# Brain Dump — 2026-04-17-000200
**Phase**: Phase 3-7 deep triage on new paid account CSV — cross-account IDOR mapping
**Session time**: ~50 min total

## What I Just Did
Confirmed both test accounts are present in the new CSV. Extracted Account B's (ted lasso) distinct CSRF key and mapped all their requests. Discovered `action=registrar` endpoint (undocumented). Full cross-account object mapping complete.

## Two-Account Map

| Field | Account A (roy kent) | Account B (ted lasso) |
|---|---|---|
| Email | `0xcaphe+roy@wearehackerone.com` | `0xcaphe+ted@wearehackerone.com` |
| member_id | `1010395` | `1015003` |
| CSRF Key | `KoU12...` / `BKe9...` | `VA7xTimlfR3wN4BxB6pK2EyJjvWJCloNRNbvXPEhrdM` |
| Domain ID | `118702` | `118689` |
| dkim_selector | `s1010395` | `s1015003` |
| Template ID | `1250783` | unknown |
| team hash_id | `e9612426...` | `33c5af9e...` |

Both accounts are on `app-us.smtp2go.com`. Domain IDs differ by 13 (sequential).

## KEY NEW FINDING: `action=registrar` — Undocumented SSRF

`GET /api/settings/verified_senders/?action=registrar&domain=wearehackerone.com`
Response: `{"registrar":"One.com"}`

This does a server-side WHOIS/registrar lookup on the domain parameter. NOT in the /end-points/ documentation. This is a direct SSRF candidate:
- Test `domain=localhost`
- Test `domain=169.254.169.254` (cloud metadata)
- Test `domain=internal-hostname.local`
- The request may be via DNS or HTTP — need to determine

## SSTI Confirmation Target

Template created with `{{ first_name }}` body. The `render-text` endpoint renders this serverside and returns `rendered_text`. The 500 error on malformed input confirmed server-side processing.

**SSTI attack plan**:
1. Create template with body: `{{config}}` or `{{''.__class__.__mro__}}`
2. POST to `render-text` with that template_id and any variables
3. If `config` dict is returned → Jinja2 SSTI confirmed
4. Escalate: `{{''.__class__.__mro__[1].__subclasses__()[XXX].__init__.__globals__['__builtins__']['__import__']('os').popen('id').read()}}`

Alternatively: inject SSTI payload as a `template_variables` KEY — if the key name is rendered in an error message, SSTI may trigger there.

## IDOR Test Matrix

### Cross-account domain IDOR (HIGH priority)
**Test 1**: Account A's session + `domain=wearehackerone.com` (owned by B)
- DELETE `/api/settings/verified_senders/` + `domain=wearehackerone.com` while logged as A
- Confounded: both accounts own the same domain name
- Better: use domain_id=118689 (B's) if any endpoint accepts integer domain_id

**Test 2**: `/api/settings/verified_senders/?action=registrar&domain=wearehackerone.com` works for both accounts — no IDOR issue, but test with target=internal service.

### Cross-account template IDOR (HIGH priority)  
**Test**: Account B calls `POST /api/settings/templates/?action=render-text` with `user_template_id=1250783` (Account A's template)
- If response contains Account A's template body → IDOR confirmed (info disclosure)

### Billing details access (MEDIUM)
Account B's subscription endpoint works — verify if member_id or session controls isolation.

## New Undocumented Endpoints Found (from action= parameter enumeration)
- `action=registrar` — domain registrar lookup (SSRF surface)
- `action=load-usernames` — loads username lists
- `action=load-all-active-users` — loads all users including hidden
- `action=get_edit_token` — issues JWT edit tokens
- `action=update_member` — requires edit_token JWT
- `action=validate-subdomains` — validates DNS subdomain configuration
- `action=get-shareable-entri-url` — generates goentri.com share links
- `action=fetch-entri-info` — fetches Entri JWT for domain verification
- `action=get-billing-details` — billing details

## Attack Priority Ranking (with signal level)

| Rank | Attack | Signal | Why |
|------|--------|--------|-----|
| 1 | SSTI via render-text template body | ⭐⭐⭐⭐⭐ | Jinja2 syntax confirmed, render endpoint returns rendered output, Python/gunicorn backend |
| 2 | SSRF via action=registrar | ⭐⭐⭐⭐ | Server-side domain lookup, undocumented, arbitrary domain input |
| 3 | SSRF via action=verify + domain | ⭐⭐⭐⭐ | Server-side DNS lookup + SSL provisioning HTTP fetch confirmed |
| 4 | IDOR template render-text (cross-account) | ⭐⭐⭐ | Integer template IDs, cross-account read of template content |
| 5 | IDOR domain objects (same domain name confounds) | ⭐⭐⭐ | Sequential domain IDs, but domain name used instead of ID |
| 6 | Stored XSS via feedback_html / template body | ⭐⭐⭐ | User-controlled HTML stored, rendered in UI |
| 7 | API key proliferation (generated_key) | ⭐⭐⭐ | Multiple keys visible, unclear if all active |
| 8 | WCD on /static/ endpoints | ⭐⭐ | Static files with ?version= param, no user-specific data |
| 9 | CSRF on state-changing endpoints | ⭐⭐ | CSRF token in query string, but required for all mutations |
| 10 | postMessage (vendor libs) | ⭐ | Only in vendor.js, infrastructure pattern not app-level |

## What Failed / Dead Ends  
- XSS via domain add — rejected ("invalid domain")
- postMessage in app-level code — only found in vendor/polyfill code, not application logic
- WCD — all API endpoints have no-store, static files have no user-sensitive data
- VLD endpoint — not seen in traffic at all
- Account B has no subaccounts yet so subaccount IDOR can't be tested from traffic

## Next Actions (immediate)
1. Test SSTI: create template with `{{7*7}}`, call render-text, observe response
2. Test SSRF: `GET /api/settings/verified_senders/?action=registrar&domain=127.0.0.1`
3. Test template IDOR: Account B session + render-text for template 1250783
4. Test domain IDOR: enumerate domain IDs ±5 from 118689/118702

## Open Questions
- Does `action=registrar` use HTTP or DNS for lookup?
- Is `render-text` sandboxed (Jinja2 sandbox) or full Jinja2?
- Are undiscovered `action=` values on other endpoints equally useful?
- What's `show_hidden_endpoints: true` in the api_users POST body?
