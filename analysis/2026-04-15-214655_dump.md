# Brain Dump — 2026-04-15-214655
**Phase**: XSS-006 testing — update_member endpoint debugging
**Session time**: ~60 min total on XSS testing

## What I Just Did
- Confirmed `get_edit_token` works with Account A password (`8gRR&8Q5P5YRDtq4q$W6`)
- Confirmed NO CSP on authenticated pages
- Systematically tried every combination of fields / methods / headers for `update_member`
- Confirmed `change-orgname` action works fine (returns OK) — JSON parsing works
- Confirmed `reinvite` and `delete-member` correctly return unexpected errors (proof of life)
- Identified: `update_member` returns 500 on first call (server-side unhandled exception), then "Malformed JSON" on subsequent calls because the server-side session marks the edit_token as consumed after the 500

## What I Found

### Root cause of update_member failure
The Django `session` cookie (`edf55abf-87ad-4b6c-85ec-93a86ffa473e`) identifies a SINGLE server-side session. Every Python `requests.Session` I create reuses the same Django session. When `update_member` throws a 500, the server stores some state in the session that causes subsequent calls to return "Malformed JSON payload" (their generic error for invalid session state / bad edit_token).

**The browser has a pristine copy of this session** — the Vue app has never triggered `update_member` from the browser, so the server-side session's edit_token is not polluted by my failed API attempts.

### What the 500 on update_member means
Something in the server-side logic for `update_member` fails (unhandled exception). This is NOT a JSON parsing issue — earlier tests confirmed JSON parsing works fine for this endpoint. The 500 is a server bug that triggers when:
- All required fields are present
- Action is `update_member`
- Account A (roy kent, solo owner) tries to update themselves

**Hypothesis**: The server throws an unhandled exception when processing `notification_level: 15` or `role: 8` for a solo owner. Possibly a Django ORM query that tries to check for at least one other owner remaining before updating, and fails because of how it processes the single-member scenario.

### XSS-006 still unconfirmed
The rendering is clear from source code — single-quoted JS string, no HTML/JS encoding, server template renders fullname directly. But we need to confirm:
1. Can the fullname field be SET to a JS injection payload via the API?
2. Does it render unescaped in the browser?

## What Failed / Dead Ends
- All programmatic update_member attempts → 500 (then "Malformed JSON" on repeat calls)
- Account B has different password (unknown)
- All method variations (PUT, PATCH), content-type variations, role format variations failed same way

## Current Hypothesis
The JS injection in `userFullName` is a real attack surface based on:
1. Source evidence: unencoded single-quoted string in `<script>` block across 19 pages
2. The `change-orgname` action works (server-side JS injection via `organisation_name` is POSSIBLE if it renders anywhere, but it doesn't appear in any page source)
3. The update_member 500 might be environment-specific (maybe only this account / this session state)

**Next step**: user tests manually in browser to confirm (see below)

## Next 5 Minutes
Provide user with browser console payload to test XSS-006 directly, since the Python-based API testing is blocked by session state corruption.

## Open Questions
- Why does update_member throw 500 specifically for Account A? Could be related to the test account being special (HackerOne signup, solo owner, TRIAL plan)
- Does Account B's fullname update work via the UI?
- Is there server-side HTML/JS encoding on the fullname output? Only browser test can confirm.
