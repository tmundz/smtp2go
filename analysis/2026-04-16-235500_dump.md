# Brain Dump — 2026-04-16-235500
**Phase**: Phase 1-3 triage on new paid account CSV (2026-04-16-232635_csv_requests.csv)
**Session time**: ~25 min

## What I Just Did
Parsed the new 10810-row CSV from the paid account (`app-us.smtp2go.com`). Decoded all base64 raw request/response fields. Enumerated all unique API paths, decoded all response bodies, and extracted all POST/PUT actions with full bodies.

## What I Found

### Account Context
- **Account A (paid/US)**: `0xcaphe+roy@wearehackerone.com` — member_id: `1010395` — `app-us.smtp2go.com`
- **Account B (free?)**: `0xcaphe+ted@wearehackerone.com` — member_id: `1015003` — seen in old CSV + zdesk JWT in new CSV
- **CSRF Key (session 1)**: `KoU12MtjCA+SESDjtsuzj38WzEYXQVF+NjA4NJaU5vU`
- **CSRF Key (session 2)**: `BKe9uceYV+4tD90J2rbt6r2AvaEKYkFCtUtbUI550Ew`

### Full Backend API Map (from `/end-points/`)
Captured complete backend API surface including previously unknown endpoints:
- `/api/dashboard/edit_account/dpa` — DPA editing
- `/api/dashboard/get_billing_history/`
- `/api/dasboard/invoice/` — *TYPO in URL: "dasboard"* — could be legacy/hidden
- `/api/account/close/` — account closure
- `/api/settings/ipallowlist/`
- `/api/settings/authentication/list/`
- `/api/settings/settings_bounce_suppress/`
- `/api/settings/two_factor_auth/`
- `/api/settings/footer/`, `/api/settings/archives/`
- `/api/settings/unsubscribe/`, `/api/settings/sendingoptions/`
- `/login-vld` — VLD authentication (unknown type)

### CRITICAL: SSTI VECTOR — Template Render Endpoint
**Endpoint**: `POST /api/settings/templates/?action=render-text`
**Body**: `user_template_id=1250783&template_variables={"first_name":"VALUE"}`
**Evidence**: Template HTML body uses `{{ first_name }}` — confirmed Jinja2/Django syntax
**SSTI potential**: `template_variables` JSON values are rendered into the template.
- First attempt: "No template exists" (template not yet created)
- Second attempt with malformed JSON (`""<h1>trsdt</h1>""`: **HTTP 500** — suggests server-side parsing/rendering triggered an internal error
- Template created successfully with `{{ first_name }}` in body

**Test plan**: Create template with `{{7*7}}` in body or inject `{{7*7}}` as a variable value and call render-text → if response contains `49`, SSTI confirmed.

### CRITICAL: API Key Generation on Every Page Load
**Endpoint**: `POST /api/settings/api_users/?action=load-active-users`
Every response returns `"generated_key":{"key":"api-XXXX","hash":"..."}` with a NEW key.
Multiple unique keys observed across requests: `api-91599ADC...`, `api-1B69DE36...`, `api-CB445463...`, `api-F7D43FCC...`, `api-4417C208...`, `api-38EC1D52...`

**Questions**: Are these keys being stored/activated, or just pre-generated for display? If stored, this is key proliferation. If shown before user saves, the question is what happens to the previously generated keys.

### HIGH: IDOR — Domain Objects (Sequential IDs)
Domain IDs from both accounts discovered:
- Account B (member 1015003): `domain.id = 118689`
- Account A (member 1010395): `domain.id = 118702`

Sequential difference of 13. The DELETE endpoint takes `domain=NAME` (not ID), but other operations may use ID. The `verify` action triggers server-side DNS lookup on the domain name — potential SSRF pivot.

### HIGH: Template IDOR  
`render-text` takes `user_template_id=1250783` — integer ID.
Template was created in Account A session. Test: can Account B read/render Account A's template?

### MEDIUM: SSRF — Domain Verify DNS Lookup
`POST /api/settings/verified_senders/?action=verify&domain=TARGET`
Server confirmed to perform DNS lookups: resolves CNAME/TXT records from external DNS (162.159.0.31 = Cloudflare). 
**Test**: Set domain to an internal hostname or redirect chain → internal services.

### MEDIUM: Zendesk Chat JWT — External ID Prediction
`POST /zdesk_chat_auth` returns JWT with `external_id: "MEMBER_ID_email"`. 
JWT uses HS256 — signed server-side, not client-forgeable without key. But the external_id format `1010395_0xcaphe+roy@...` is predictable.

### MEDIUM: Subdomain Parameters in entri-url
`POST /api/settings/verified_senders/?action=get-shareable-entri-url`
Body: `esl_rpath_subdomain=em1010395&esl_dkim_subdomain=s1010395._domainkey&esl_tracker_subdomain=link`
These subdomain values are user-controlled and returned in a goentri.com URL. Potential for subdomain injection affecting email routing.

### Server Stack
- **Backend**: Python/gunicorn (Server: gunicorn header)
- **Template engine**: Jinja2 or Django templates (confirmed `{{ var }}` syntax)
- **Session**: `login_id` cookie (Werkzeug/Flask signed session format)
- **Auth**: Cookie-based with CSRF key in query params (not header!)
- **CDN**: No CF-Cache-Status headers on API responses — `cache-control: no-cache, no-store, must-revalidate` on all API routes

## What Failed / Dead Ends
- XSS in domain field: Rejected with "invalid domain" — server-side validation
- Template render-text with malformed JSON: 500 error but no SSTI confirmation yet (template didn't exist)
- WCD: All API routes have `no-store` — unlikely WCD on API endpoints

## Current Hypothesis
**SSTI is the highest-priority lead.** The `render-text` endpoint processes user-controlled template variables through what appears to be Jinja2. A 500 error on malformed input suggests the rendering pipeline is being reached. With a valid template containing `{{7*7}}` or with `template_variables` containing a Jinja2 probe, RCE may be achievable.

**IDOR on domain IDs** is the second priority — sequential IDs between accounts make this testable.

## Next 5 Minutes
1. Look at api.smtp2go.com traffic (REST API) for additional IDOR vectors
2. Look at static riot.js tag content for postMessage / XSS sinks
3. Check for `subaccount_id` parameters and the login-to-subaccount flow
4. Extract more unique action parameters from all endpoints

## Open Questions
- Does `POST /api/settings/templates/?action=render-text` render the `template_variables` VALUES through the template engine, or just substitute them as string literals?
- Are generated API keys from load-active-users calls persisted to the database?
- What does `/login-vld` do? VLD = ?
- The `show_hidden_endpoints: true` parameter in api_users — what does it reveal?
