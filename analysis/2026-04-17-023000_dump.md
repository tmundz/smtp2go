# Brain Dump — 2026-04-17-023000
**Phase**: Live SSRF testing — DNS hit confirmed, source IP identified
**Session time**: ~15 min

## CONFIRMED: DNS SSRF via action=registrar

**Source IP**: `18.209.86.113` — AWS EC2 us-east-1
**Query type**: A record for `jditqtchormwvxuggpxvegjqw9ehrg4k5.oast.fun`
**Time**: 2026-04-17 12:12 UTC
**Triggered by**: `GET /api/settings/verified_senders/?action=registrar&domain=<COLLAB>`

### Why AWS matters
Server is NOT on Linode — it's **AWS EC2**. This means:
- Cloud metadata endpoint: `http://169.254.169.254/latest/meta-data/`
- IMDSv1 is unauthenticated — just a GET to 169.254.169.254
- Credentials available at: `http://169.254.169.254/latest/meta-data/iam/security-credentials/`
- BUT: action=registrar is DNS-only (NS/A lookup), not HTTP — can't reach 169.254.169.254 via this vector

### DNS SSRF severity
- Current: ✅ Blind DNS SSRF — attacker can force server to resolve arbitrary domains
- Impact: DNS exfiltration, SSRF confirmation — Medium severity as standalone
- Not exploitable for metadata access (DNS != HTTP)

## Webhook creation — needs UI
API endpoint returns 500 (likely migrated to Svix backend).
Must create via UI at /settings/webhooks/.
Once webhook test is triggered → HTTP SSRF confirmed.

## Next: HTTP SSRF escalation path (if webhook confirms fetch)
1. Create webhook to interactsh → confirm HTTP POST received
2. Update webhook URL to http://169.254.169.254/latest/meta-data/iam/security-credentials/
3. Trigger test → response body returned in testResults[].response
4. If response contains IAM role credentials → CRITICAL

## Source IP to note for report
`18.209.86.113` — aws ec2, us-east-1 region

## UPDATE — Webhook HTTP hit received

**Source IP: 50.112.21.217** — this is **Svix infrastructure**, NOT smtp2go's own server.
- User-Agent: `Svix-Webhooks/1.84.0 (sender-9YMgn; +https://www.svix.com/http-sender/)`
- smtp2go outsources webhook delivery to Svix (third-party service)
- 169.254.169.254 via webhook → hits Svix's own EC2 metadata, not smtp2go's
- **Webhook SSRF severity is reduced**: server-side fetch is on Svix's infrastructure, not smtp2go's

## Two distinct SSRF vectors now confirmed

| Vector | Source IP | Protocol | Hits smtp2go's internal network? |
|--------|-----------|----------|-----------------------------------|
| action=registrar | 18.209.86.113 (smtp2go AWS) | DNS only | Yes (DNS only) |
| Webhook test | 50.112.21.217 (Svix AWS) | HTTP POST | No (Svix infra) |
| action=verify | TBD | DNS + HTTP? | Likely yes — needs confirm |

## Next: action=verify HTTP fetch from smtp2go's own server
Domain jditqtchormwvxuggpxvegjqw9ehrg4k5.oast.fun added and verify triggered.
The dkim_status response showed DNS lookups hitting 206.189.156.69 (oast.fun resolver).
Need to see if an HTTP fetch from smtp2go's own server (18.209.86.113 range) also hits interactsh.
